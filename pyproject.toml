[project]
name = "selfupdatingai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.13.0",
    "transformers>=5.15.1",
]
